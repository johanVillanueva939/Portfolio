import { Network } from "../../UI/Network/Network"

export const Networks = () => {
    return (
            <div className="fixed top-[30%] left-0 h-[200px] w-1 clip-path-custom bg-transparent flex items-center justify-center">
                <Network />
            </div>
    )
}

export default Networks