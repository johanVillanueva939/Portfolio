import { Network } from "../../UI/Network/Network"

export const Networks = () => {
    return (
            <div className="fixed top-[-10px] left-[50%] h-[170px] w-2 clip-path-custom bg-transparent flex items-center justify-center rotate-[90deg]">
                <Network />
            </div>
    )
}

export default Networks